The Language calc
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of calc==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.


Integer literals //Integer// are nonempty sequences of digits.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.





ClassName literals are recognized by the regular expression
`````upper letter+`````

Negative literals are recognized by the regular expression
`````'-' digit+`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in calc are the following:
  | ``abs`` | ``and`` | ``any`` | ``apply``
  | ``bool`` | ``case`` | ``class`` | ``double``
  | ``downto`` | ``else`` | ``elseif`` | ``false``
  | ``for`` | ``if`` | ``int`` | ``loop``
  | ``not`` | ``or`` | ``print`` | ``println``
  | ``read`` | ``scope`` | ``sqrt`` | ``switch``
  | ``text`` | ``to`` | ``true`` | 

The symbols used in calc are the following:
  | [ | ] | , | ;
  | ( | ) | = | .
  | += | *= | -= | /=
  | { | } | : | ==
  | != | < | > | <=
  | >= | + | - | *
  | / | % | ^ | ++
  | -- | << | >> | :=
  | | | :: | -> | 

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of calc==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Prog// | -> | //[Inst]// 
  | //Type// | -> | //ClassName// 
  |  |  **|**  | ``int`` 
  |  |  **|**  | ``text`` 
  |  |  **|**  | ``any`` 
  |  |  **|**  | ``bool`` 
  |  |  **|**  | ``double`` 
  |  |  **|**  | //Type// //Array// 
  | //Array// | -> | ``[`` //Exp// ``]`` 
  | //[Type]// | -> | //Type// 
  |  |  **|**  | //Type// ``,`` //[Type]// 
  | //[Inst]// | -> | **eps**
  |  |  **|**  | //Inst// //[Inst]// 
  | //Inst// | -> | //Decl// 
  |  |  **|**  | //Stmt// 
  |  |  **|**  | //Exp// 
  |  |  **|**  | //Loop// 
  |  |  **|**  | //For// 
  |  |  **|**  | //Fun// 
  |  |  **|**  | //ClassHeader// 
  |  |  **|**  | ``apply`` //Ident// //Ident// 
  |  |  **|**  | ``apply`` //Lambda// //Ident// ``;`` 
  |  |  **|**  | ``print`` ``(`` //Exp// ``)`` ``;`` 
  |  |  **|**  | ``println`` ``(`` //Exp// ``)`` ``;`` 
  |  |  **|**  | ``read`` ``(`` //Ident// ``)`` ``;`` 
  | //[Decl]// | -> | **eps**
  |  |  **|**  | //Decl// //[Decl]// 
  | //[Stmt]// | -> | **eps**
  |  |  **|**  | //Stmt// //[Stmt]// 
  | //Decl// | -> | //Type// //[Var]// ``;`` 
  | //Var// | -> | //Ident// 
  |  |  **|**  | //Ident// ``=`` //Exp// 
  | //[Var]// | -> | //Var// 
  |  |  **|**  | //Var// ``,`` //[Var]// 
  | //Stmt// | -> | //Ident// ``=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``.`` //Ident// ``=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``.`` //Ident// ``[`` //Exp// ``]`` //[Elem]// ``=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``[`` //Exp// ``]`` //[Elem]// ``=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``[`` //Exp// ``]`` //[Elem]// ``.`` //Ident// ``=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``+=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``*=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``-=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``/=`` //Exp// ``;`` 
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` //Inst// //[ElIf]// 
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` //Inst// //[ElIf]// ``else`` //Inst// 
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` //Inst// //[ElIf]// ``else`` ``{`` //[Inst]// ``}`` 
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` ``{`` //[Inst]// ``}`` //[ElIf]// 
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` ``{`` //[Inst]// ``}`` //[ElIf]// ``else`` //Inst// 
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` ``{`` //[Inst]// ``}`` //[ElIf]// ``else`` ``{`` //[Inst]// ``}`` 
  |  |  **|**  | ``switch`` ``(`` //Exp// ``)`` ``{`` //[Case]// ``}`` ``else`` ``{`` //[Inst]// ``}`` 
  |  |  **|**  | ``switch`` ``(`` //Exp// ``)`` ``{`` //[Case]// ``}`` 
  | //ElIf// | -> | ``elseif`` ``(`` //Exp// ``)`` //Inst// 
  |  |  **|**  | ``elseif`` ``(`` //Exp// ``)`` ``{`` //[Inst]// ``}`` 
  | //[ElIf]// | -> | **eps**
  |  |  **|**  | //ElIf// //[ElIf]// 
  | //Case// | -> | ``case`` //Exp// ``:`` ``{`` //[Inst]// ``}`` 
  | //[Case]// | -> | **eps**
  |  |  **|**  | //Case// //[Case]// 
  | //[Exp]// | -> | **eps**
  |  |  **|**  | //Exp// 
  |  |  **|**  | //Exp// ``,`` //[Exp]// 
  | //Exp18// | -> | ``true`` 
  |  |  **|**  | ``false`` 
  |  |  **|**  | //Ident// 
  |  |  **|**  | //Exp19// 
  | //Exp2// | -> | //Exp2// ``and`` //Exp3// 
  |  |  **|**  | //Exp2// ``or`` //Exp3// 
  |  |  **|**  | //Exp3// 
  | //Exp13// | -> | ``not`` //Exp13// 
  |  |  **|**  | ``sqrt`` ``(`` //Exp13// ``)`` 
  |  |  **|**  | ``abs`` //Exp13// 
  |  |  **|**  | //Exp14// 
  | //Exp8// | -> | //Exp8// ``==`` //Exp9// 
  |  |  **|**  | //Exp8// ``!=`` //Exp9// 
  |  |  **|**  | //Exp9// 
  | //Exp9// | -> | //Exp9// ``<`` //Exp10// 
  |  |  **|**  | //Exp9// ``>`` //Exp10// 
  |  |  **|**  | //Exp9// ``<=`` //Exp10// 
  |  |  **|**  | //Exp9// ``>=`` //Exp10// 
  |  |  **|**  | //Exp10// 
  | //Exp10// | -> | //Exp10// ``+`` //Exp11// 
  |  |  **|**  | //Exp10// ``-`` //Exp11// 
  |  |  **|**  | //Exp11// 
  | //Exp11// | -> | //Exp11// ``*`` //Exp12// 
  |  |  **|**  | //Exp11// ``/`` //Exp12// 
  |  |  **|**  | //Exp11// ``%`` //Exp12// 
  |  |  **|**  | //Exp12// 
  | //Exp12// | -> | //Exp12// ``^`` //Exp13// 
  |  |  **|**  | //Exp13// 
  | //Exp14// | -> | ``++`` //Exp14// 
  |  |  **|**  | ``--`` //Exp14// 
  |  |  **|**  | //Exp15// 
  | //Exp15// | -> | //Exp15// ``++`` 
  |  |  **|**  | //Exp15// ``--`` 
  |  |  **|**  | //Exp16// 
  | //Exp16// | -> | ``<<`` //[Exp]// ``>>`` 
  |  |  **|**  | //String// 
  |  |  **|**  | //Integer// 
  |  |  **|**  | //Double// 
  |  |  **|**  | //Call// 
  |  |  **|**  | //Negative// 
  |  |  **|**  | ``scope`` ``.`` //Ident// 
  |  |  **|**  | //Exp17// 
  | //Exp17// | -> | //Ident// ``.`` //Ident// 
  |  |  **|**  | //Exp18// 
  | //Exp19// | -> | //Ident// ``[`` //Exp// ``]`` //[Elem]// 
  |  |  **|**  | //Ident// ``[`` //Exp// ``]`` //[Elem]// ``.`` //Ident// 
  |  |  **|**  | //Ident// ``.`` //Ident// ``[`` //Exp// ``]`` //[Elem]// 
  |  |  **|**  | //Lambda// 
  |  |  **|**  | //Exp20// 
  | //Elem// | -> | ``[`` //Exp// ``]`` 
  | //[Elem]// | -> | **eps**
  |  |  **|**  | //Elem// //[Elem]// 
  | //Exp// | -> | //Exp1// 
  | //Exp1// | -> | //Exp2// 
  | //Exp3// | -> | //Exp4// 
  | //Exp4// | -> | //Exp5// 
  | //Exp5// | -> | //Exp6// 
  | //Exp6// | -> | //Exp7// 
  | //Exp7// | -> | //Exp8// 
  | //Exp20// | -> | ``(`` //Exp// ``)`` 
  | //For// | -> | ``for`` //Type// //Ident// ``:=`` //Exp// ``to`` //Exp// //Inst// 
  |  |  **|**  | ``for`` //Type// //Ident// ``:=`` //Exp// ``to`` //Exp// ``{`` //[Inst]// ``}`` 
  |  |  **|**  | ``for`` //Type// //Ident// ``:=`` //Exp// ``downto`` //Exp// //Inst// 
  |  |  **|**  | ``for`` //Type// //Ident// ``:=`` //Exp// ``downto`` //Exp// ``{`` //[Inst]// ``}`` 
  | //Loop// | -> | ``loop`` ``(`` //LoopVars// //Exp// //LoopHow// ``)`` //Inst// 
  |  |  **|**  | ``loop`` ``(`` //LoopVars// //Exp// //LoopHow// ``)`` ``{`` //[Inst]// ``}`` 
  | //LoopVars// | -> | //[LoopDecl]// ``;`` 
  |  |  **|**  | **eps**
  | //LoopHow// | -> | ``;`` //[IterExp]// 
  |  |  **|**  | **eps**
  | //[LoopDecl]// | -> | **eps**
  |  |  **|**  | //LoopDecl// 
  |  |  **|**  | //LoopDecl// ``|`` //[LoopDecl]// 
  | //LoopDecl// | -> | //Type// //[VarLoop]// 
  | //VarLoop// | -> | //Ident// ``=`` //Exp// 
  | //[VarLoop]// | -> | //VarLoop// 
  |  |  **|**  | //VarLoop// ``,`` //[VarLoop]// 
  | //[IterExp]// | -> | **eps**
  |  |  **|**  | //IterExp// 
  |  |  **|**  | //IterExp// ``,`` //[IterExp]// 
  | //IterExp// | -> | //Exp// 
  |  |  **|**  | //LoopStmt// 
  | //LoopStmt// | -> | //Ident// ``+=`` //Exp// 
  |  |  **|**  | //Ident// ``*=`` //Exp// 
  |  |  **|**  | //Ident// ``-=`` //Exp// 
  |  |  **|**  | //Ident// ``/=`` //Exp// 
  | //Fun// | -> | //Ident// ``::`` //ParamsList// ``{`` //[Inst]// ``}`` 
  | //ParamsList// | -> | ``[`` //[FunArgs]// ``]`` ``->`` ``[`` //[FunArgs]// ``]`` 
  | //FunArgs// | -> | //Type// //Ident// 
  |  |  **|**  | //Ident// ``::`` //ParamsList// 
  | //[FunArgs]// | -> | **eps**
  |  |  **|**  | //FunArgs// 
  |  |  **|**  | //FunArgs// ``,`` //[FunArgs]// 
  | //FunParam// | -> | //Ident// ``::`` ``(`` //ParamsList// ``)`` 
  | //Call// | -> | //Ident// ``(`` //[RealParam]// ``)`` 
  | //RealParam// | -> | //Exp// 
  | //[RealParam]// | -> | **eps**
  |  |  **|**  | //RealParam// 
  |  |  **|**  | //RealParam// ``,`` //[RealParam]// 
  | //Lambda// | -> | ``(`` //Type// ``::`` ``[`` //[LambdaParams]// ``]`` ``->`` ``(`` //Exp// ``)`` ``)`` 
  | //LambdaParams// | -> | //Type// //Ident// 
  | //[LambdaParams]// | -> | **eps**
  |  |  **|**  | //LambdaParams// 
  |  |  **|**  | //LambdaParams// ``,`` //[LambdaParams]// 
  | //ClassHeader// | -> | ``class`` //ClassName// ``{`` //[ClassStmt]// ``}`` 
  | //ClassBlock// | -> | ``{`` //[ClassStmt]// ``}`` 
  | //ClassStmt// | -> | //Decl// 
  | //[ClassStmt]// | -> | **eps**
  |  |  **|**  | //ClassStmt// //[ClassStmt]// 


